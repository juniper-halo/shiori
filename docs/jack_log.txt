date [2026-04-03], focus [LLM setup, adapter development], log [Designed and stored the Baymax-style system prompt under config/ and wired LLM_SYSTEM_PROMPT_PATH through config validation and env example. Implemented llm_adapter with libcurl plus vendored cJSON: OpenAI-compatible chat completions, 40-second timeout, shutdown flag for non-blocking abort, and shape tests. Selected SmolLM2-1.7B-Instruct Q4_K_M, documented models/ as gitignored local GGUF storage, and verified llama-server on Apple Silicon with POST /v1/chat/completions returning 200. Updated HUMANS.md and AGENTS.md to describe architecture, build deps, and runtime commands for CMake and llama-server.], good luck
date [2026-05-03], focus [audio driver implementation — config split, capture, playback, BT OS setup], log [Split AUDIO_DEVICE into AUDIO_CAPTURE_DEVICE + AUDIO_PLAYBACK_DEVICE throughout config.h/config.c/env.example; updated test_config_shape.c with parsing and validation coverage. Implemented audio_capture.c: fork/execvp arecord with S16_LE 16 kHz 1-ch PCM pipe, SIGTERM stop with 2 s poll + SIGKILL escalation, EINTR/EAGAIN-safe read, child reap on EOF. Implemented playback.c: fork/execvp aplay with 30 s blocking timeout, SIGTERM escalation, non-zero exit mapped to ASSISTANTD_ERR_CHILD_PROCESS. Added test_audio_capture_shape.c and test_playback_shape.c; all 6 tests pass on macOS without hardware. Implemented Bluetooth OS setup in install_assistantd.sh: --setup-bluetooth flag with --bt-mac, --bt-stack (bluealsa|pipewire), --skip-bt-pair, --update-bt-config; retried bluetoothctl connect, audio validation via aplay. Deployed to Pi (Bookworm/PipeWire) via rsync+SSH; confirmed aplay -D pulse exits 0 against PipeWire null sink — playback.c end-to-end validated on device.], good luck
